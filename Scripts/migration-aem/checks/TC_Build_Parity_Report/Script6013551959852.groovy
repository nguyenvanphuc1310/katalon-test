import migration.ReportBuilder

// Reports/parity-report/ — index.html, one file per template, one file per page.
// Reads the results the checks wrote to disk; renders nothing else.
ReportBuilder.build()
